// Minimal security library exports
// TODO: Re-enable full functionality after resolving TypeScript configuration issues

export interface ISigningService {
  // Placeholder interface for signing service
}

export interface IKmsService {
  // Placeholder interface for KMS service
}

export interface IAuthService {
  // Placeholder interface for authentication service
}