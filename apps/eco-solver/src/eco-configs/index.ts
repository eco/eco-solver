// This file provides backward compatibility for existing imports
// TODO: Remove this compatibility layer in v2.0.0 after migration is complete

// All eco-config imports now redirect to @libs/solver-config via tsconfig paths
// No additional compatibility layer needed - imports work transparently
