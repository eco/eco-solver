{
  "name": "eco-solver",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/eco-solver/src",
  "projectType": "application",
  "tags": ["scope:eco-solver", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"],
        "cwd": "apps/eco-solver"
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        },
        "preproduction": {
          "args": ["--node-env=preproduction"]
        },
        "staging": {
          "args": ["--node-env=staging"]
        }
      }
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/eco-solver/package.json",
        "{workspaceRoot}/dist/apps/eco-solver/package-lock.json"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": ["{workspaceRoot}/dist/apps/eco-solver/workspace_modules"],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "nx:run-commands",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "command": "NODE_CONFIG_DIR=apps/eco-solver/config node $(find .nx/cache -name 'main.js' -path '*/dist/apps/eco-solver/main.js' | head -1)"
      },
      "configurations": {
        "development": {
          "command": "NODE_CONFIG_DIR=apps/eco-solver/config NODE_ENV=development node $(find .nx/cache -name 'main.js' -path '*/dist/apps/eco-solver/main.js' | head -1)"
        },
        "production": {
          "command": "NODE_CONFIG_DIR=apps/eco-solver/config NODE_ENV=production node $(find .nx/cache -name 'main.js' -path '*/dist/apps/eco-solver/main.js' | head -1)"
        },
        "preproduction": {
          "command": "NODE_CONFIG_DIR=apps/eco-solver/config NODE_ENV=preproduction node $(find .nx/cache -name 'main.js' -path '*/dist/apps/eco-solver/main.js' | head -1)"
        },
        "staging": {
          "command": "NODE_CONFIG_DIR=apps/eco-solver/config NODE_ENV=staging node $(find .nx/cache -name 'main.js' -path '*/dist/apps/eco-solver/main.js' | head -1)"
        }
      }
    },
    "test": {
      "options": {
        "passWithNoTests": true
      }
    },
    "cli": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node src/commander/command-main.ts",
        "cwd": "apps/eco-solver",
        "env": {
          "NODE_CONFIG_DIR": "./config"
        }
      }
    }
  }
}
