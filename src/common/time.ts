export const ONE_MINUTE = 60
export const ONE_HOUR = ONE_MINUTE * 60
export const ONE_DAY = ONE_HOUR * 24

export const hours = (hours: number) => hours * ONE_HOUR
export const minutes = (minutes: number) => minutes * ONE_MINUTE
export const days = (days: number) => days * ONE_DAY
